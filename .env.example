# This file provides a template for the required environment variables.
# For local development, copy this file to .env.local and fill in the values.
# DO NOT commit your .env.local file to version control.

# --- ADMIN USER ---
# The email address for the first user, who will be granted super-admin privileges.
NEXT_PUBLIC_ADMIN_EMAIL="your-admin-email@example.com"

# --- GOOGLE AI (for Genkit) ---
# Your API key from Google AI Studio.
GOOGLE_API_KEY="YOUR_API_KEY_HERE"

# --- FIREBASE CLIENT SDK (for browser) ---
# These values come from your Firebase project's web app configuration.
# (Project Settings -> General -> Your apps -> Web app)
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_FIREBASE_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-auth-domain.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-storage-bucket.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"

# --- FIREBASE ADMIN SDK (for server) ---
# For local development, this points to your downloaded service account key.
# For production, you should set the FIREBASE_SERVICE_ACCOUNT variable directly
# in your hosting environment's settings with the JSON content.
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

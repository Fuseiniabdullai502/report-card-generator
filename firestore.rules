rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
    function getUserData(userId) {
      return get(/databases/$(database)/documents/users/$(userId)).data;
    }
    
    function hasRole(userId, role) {
      let userData = getUserData(userId);
      return userData.role == role;
    }

    function isAdmin() {
      return requesterRole() in ['super-admin', 'big-admin', 'admin'];
    }

    function requesterRole() {
      return isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        ? get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role
        : null;
    }

    function isSignedIn() {
      return request.auth != null;
    }

    match /users/{userId} {
      // A user can read their own user doc; admins can read any
      allow get: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      // Listing / querying users is admin-only
      allow list: if isSignedIn() && isAdmin();

      // A user creates their own doc (e.g., on registration)
      allow create: if isSignedIn() && request.auth.uid == userId;

      // Updates:
      // - The user can update their own document only if they DON'T change their role
      // - Admins can update any user doc
      allow update: if isSignedIn() && (
        (request.auth.uid == userId && request.resource.data.role == resource.data.role) ||
        isAdmin()
      );

      // Only super-admins can delete user docs
      allow delete: if isSignedIn() && requesterRole() == 'super-admin';
    }
    
    match /invites/{inviteId} {
      // If you truly need public reads during registration, keep this true.
      // If you want to limit exposure, switch to "if isSignedIn()" instead.
      allow read: if true;

      // Only admins can create/update/delete invites
      allow write: if isSignedIn() && isAdmin();
    }

    match /reports/{reportId} {
      allow create: if request.auth != null;
      
      allow read, update, delete: if request.auth != null && (
        requesterRole() == 'super-admin' ||
        (requesterRole() == 'big-admin' && getUserData(request.auth.uid).district == resource.data.district) ||
        (requesterRole() == 'admin' && getUserData(request.auth.uid).schoolName == resource.data.schoolName) ||
        resource.data.teacherId == request.auth.uid
      );

      // Allow queries based on role
      allow list: if request.auth != null && (
        // Super-admin can query anything
        requesterRole() == 'super-admin' ||
        
        // Big-admin can only query their own district
        (requesterRole() == 'big-admin' && request.query.get('district') == getUserData(request.auth.uid).district) ||
        
        // Admin can only query their own school
        (requesterRole() == 'admin' && request.query.get('schoolName') == getUserData(request.auth.uid).schoolName) ||
        
        // Users (teacher) and public users can only query their own reports
        ((requesterRole() == 'user' || requesterRole() == 'public_user') && request.query.get('teacherId') == request.auth.uid)
      );
    }
  }
}
